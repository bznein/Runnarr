# Stored as /srv/runnarr/environments/production/image.env before the first
# managed promotion. The first promotion replaces this local image reference
# with the accepted immutable staging digest.
RUNNARR_IMAGE=runnarr:local
RUNNARR_DEPLOY_ENVIRONMENT=production
